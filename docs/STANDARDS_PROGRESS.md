---
title: "Standards progress"
description: "The scoreboard of the engineering standard on this repository: what each metric measures, the ratchet that holds it, the phases open, and the dated log of every deliberate change of a floor. Numbers only, never 'improved'."
category: governance
status: living
audience: ["developer", "agent"]
tags: ["standards", "ratchet", "scoreboard"]
related: ["./README.md", "./ADOPTION_DECISIONS.md"]
source_truth: ["scripts/ci/standards-baseline.json", "abatty.probes.mjs", "docs/ADOPTION_STATE.json"]
last_verified: "2026-09-24"
---

# Standards progress

## Scoreboard

| Metric | Day 0 | Now | Target | Held by | Rule |
|---|---|---|---|---|---|
| Gap-analysis score | 49/100 | 79/100 (2026-09-23, 76 checks under abatty 0.5.1; 81/100 over 75 under 0.3 on 2026-09-22, 82/100 over 74 under 0.2.0 on 2026-09-21) | 100 | `abatty measure` | - |
| Enforced share (rules held by a machine) | 74% (35/47) | 73% (46/63) | 100% | `abatty measure` | - |
| `size.overBudget` (files over their kind's budget) | 31 | 0 | 0 | hard | CODE.1 |
| `size.excessCode` (code lines over budget, summed) | 5302 | 0 | 0 | hard | CODE.1 |
| `size.overRaw` (files over the 800-line cap) | 1 | 0 | 0 | ratchet (and `max-lines` at error) | CODE.1 |
| ESLint findings (`eslint . --max-warnings=0`) | 137 (Next 16 presets) | 0 | 0 | hard (lint script) | CODE.4 |
| jsx-a11y findings (recommended, mapped) | 39 | 0 | 0 | hard (lint script) | A11Y.1 |
| axe serious/critical on the critical journeys | not measured | 0 | 0 | hard (`pnpm e2e`) | A11Y.1 |
| Token pairs under WCAG AA (both themes) | not measured (warning on muted was 4.11:1) | 0 of 36 text pairs; input border 1.46:1 vs 3:1 | 0 | hard (`pnpm contrast`) | A11Y.1 |
| `types.escapes` (`any`, `ts-ignore`) | 13 | 0 | 0 | hard | TYPES.1 |
| `types.nonNull` (a postfix `!`; abatty 0.5.2's opt-in probe) | 13 (added 2026-09-24) | 0 | 0 | hard | TYPES.1 |
| Typecheck errors with `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` | 140 (measured 2026-09-20, flags off) | 0 (flags on) | 0 | hard (`tsc --noEmit` in the gate and CI) | TYPES.1 |
| Unit coverage floor, `lib/**` (statements / branches / functions / lines) | 92.7 / 79.7 / 79.4 / 92.9 | 98.9 / 94.5 / 99.1 / 99.3 | raised with the measurement | hard (`pnpm test`) | TEST.4 |
| Integration tests on a real Postgres (rolled-back transactions) | 0 | 65 (8 files) | grows with the surface | hard (`pnpm test:integration` in CI) | TEST.2 / DATA.3 |
| `fn.shapeExemptions` (files exempted from the function-shape rules; the repository's probe) | 44 (measured 2026-09-20) | 0 | 0 | hard | CODE.2 |
| `valid.rawEnv` (`process.env` outside `lib/env.ts`) | 37 | 0 | 0 | hard | VALID.3 |
| `valid.unparsedBoundary` (boundaries with an argument no zod parse names; the repository's probe) | 40 (measured 2026-09-20) | 0 | 0 | hard | VALID.1 |
| `valid.wholeEnv` (`process.env` taken whole outside `lib/env.ts`; the repository's probe) | 2 (measured 2026-09-20) | 0 | 0 | hard | VALID.3 |
| `valid.utcDay` (a calendar day sliced or split off a UTC instant; abatty 0.3's probe) | 0 (added 2026-09-22) | 0 | 0 | hard | VALID.5 |
| `cache.serverCacheUse` (what creates a cached read on the server; the repository's probe) | 0 (measured 2026-09-20) | 0 | 0 | hard | CACHE.1 |
| `auth.unguardedAdminPage` (admin pages not re-checking the role first; the repository's probe) | 17 (measured 2026-09-20) | 0 | 0 | hard | CACHE.2 |
| `api.bareResponse` / `api.unboundedList` / `api.rowReturn` / `api.floatMoney` (the envelope, bounded lists, shaped payloads, money as a string; the repository's probes) | 5 / 2 / 36 / 7 (measured 2026-09-20) | 0 | 0 | hard | API.1 |
| Import-graph violations (`pnpm run graph`: no-circular, no-orphans, five boundary arrows) | 0 (two of the template's arrows matched nothing) | 0 (every arrow of the map held) | 0 | hard (gate) | CODE.5 |
| Dead code (`pnpm run dead`, knip: files, dependencies, exports, types) | 0 | 0 | 0 | hard (gate) | CODE.6 |
| `dup.clones` / `dup.clonedLines` (jscpd at its defaults over app, components, lib, auth.ts, proxy.ts; the repository's probes) | not measured (82 / 1064 on 2026-09-21) | 64 / 824 | falls with every split that removes a copy | ratchet | CODE.12 |
| Console calls on the server paths (`no-console` at error on lib, app, server, auth.ts, proxy.ts, instrumentation.ts) | 13 (measured 2026-09-21; 3 printing an address, 2 a signature) | 0 | 0 | hard (lint script) | OBS.1 |
| Unit coverage floor, `server/**` (the logger's redaction, the drain) | - | 100 / 100 / 100 / 100 | 100 | hard (`pnpm test`) | TEST.4 |
| `docs.frontMatter` (documents without front matter) | 2 | 0 | 0 | hard | DOC.1 |
| `docs.indexDrift` (documents missing from the index) | 1 | 0 | 0 | hard | DOC.3 |
| Exports without a JSDoc block on `lib/**`, `app/actions/**`, `app/api/**`, `auth.ts`, `proxy.ts` (`jsdoc/require-jsdoc` publicOnly, every exported declaration) | 147 (measured 2026-09-21; 121 under the arrow-only context) | 0 | 0 | hard (lint script) | CODE.7 |
| Exported components and hooks without a JSDoc block, `components/**` (functions only, not props types) | 236 (measured 2026-09-23; first reported as 351, which counted every `export default function` twice) | 0 | 0 | hard (lint script, the whole folder) | CODE.7 |
| Living docs with `source_truth` + `last_verified` (DOC-FRESHNESS) | 3/7 dated, 1 with source_truth | 9/9 dated, 6 with source_truth | every doc that describes a file | ratchet (`docs.behindCode`) | DOC.5 |
| `test.coverageExclusions` (paths taken out of the coverage count, list and inline; abatty's opt-in probe) | 11 (added 2026-09-24) | 11 | shrinks; a new entry needs a reasoned raise | ratchet | TEST.4 |
| Coverage `lib/**` statements / branches / functions / lines | - | 98.9 / 94.5 / 99.1 / 99.3 (per file: `menu.ts` 88.8 / 85 / 63.6 / 86.2, `totp.ts` 100) | raised, never lowered | `vitest` thresholds, total and changed lines | TEST.4 |

## Phase status

| # | Phase | Status |
|---|---|---|
| 0 | The instrument: ratchet, gate + hook, CI step, control cases | done 2026-09-20 |
| 1 | Lint to zero warnings | done 2026-09-20 |
| 2 | Coverage pinned | done 2026-09-20 |
| 3 | Accessibility proxies to zero | done 2026-09-20 |
| 4 | Zod: every boundary parses, one schema both sides, one env module | done 2026-09-20 |
| 5 | Caching conformance | done 2026-09-20 (not applicable for server caches, held by a probe) |
| 6 | API conformance | done 2026-09-20 |
| 7 | Function shape to zero | done 2026-09-20 |
| 8 | File size to zero, per kind | done 2026-09-21 |
| 9 | Typecheck to zero | done 2026-09-21 |
| 10 | Testing to target: real Postgres integration, branches first | done 2026-09-21 |
| 11 | Documents and JSDoc: freshness fields, the exported surface documented | done 2026-09-21 |
| 12 | The import graph and dead code held, duplication measured | done 2026-09-21 |
| 13 | Observability: the logger with its redaction, no-console, the health endpoint, the SIGTERM drain | done 2026-09-21 |

## Log

- 2026-09-24 · **`test.coverageExclusions` counted, at 11** · The coverage exclude list in `vitest.config.ts` grew one or two entries at a time with feature commits (each with its reason, each pointing to an integration suite), and nothing counted it: a 100 % floor on `server/**` holds only over what is not excluded. abatty's opt-in probe is enabled in `ratchet.enable` at main's reading of 11 entries (9 before #131 added `lib/table-tab-loader.ts` and `server/order-store.ts`, both held by `tests/integration/order-additions.test.ts`); a new exclusion now needs `abatty baseline --reason`. `next-env.d.ts` is no longer tracked, so it no longer counts among the scanned files.
- 2026-09-24 · **abatty 0.5.2, `types.nonNull` hard at zero** · The harness moves to 0.5.2 (hooks on `pnpm exec`, the commit-msg changelog check, the gate running the changed-lines coverage). Its opt-in `types.nonNull` is enabled in `ratchet.enable`: 13 non-null assertions → 0, each replaced by the check it stood for, and the baseline promotes it to hard. `lib/social.ts` gained the tests for its untested branches once touching it put them in the changed-lines set. The same day the shared zod schemas were tightened (https and host rules on stored URLs, hex colours, bounded text, the sign-in code), each rule tested in `lib/schemas/*.test.ts`.
- 2026-09-23 · **JSDoc: every component held** · The last 94 exported components and hooks documented and the rule's directory list replaced by `components/**`; 0 remain. The starting count is corrected to 236 distinct exports: the audit that reported 351 counted each `export default function` twice (once as a function, once as a default export), and the earlier log lines carry that inflated figure. Drafting the blocks found four real bugs, fixed in the same change (see the changelog).
- 2026-09-23 · **JSDoc: the forms, the uploads and the AR viewer held** · 71 blocks on `dish-form`, `restaurant-form`, `category-manager`, `client-form`, `upload` and `ar-viewer`; the six directories join the rule. 280 → 209.
- 2026-09-23 · **JSDoc: menu, shell and admin held** · 31 blocks on the public menu and its cart, the portals' shell and the People tab; the three directories join the rule's list in `eslint.config.mjs`. 311 → 280.
- 2026-09-23 · **components join the JSDoc rule, a directory at a time** · 351 exported components and hooks in `components/` had no block (measured with the project's rule over the whole tree; 53 props types and 102 `app/` page exports besides, which stay out). `components/insights`, `components/orders` and `components/waiter` documented (40 blocks, each what the component is for) and added to a second `jsdoc/require-jsdoc` block in `eslint.config.mjs`, functions only; 351 → 311. Decision in `ADOPTION_DECISIONS.md` (2026-09-23).
- 2026-09-23 · **scoreboard re-read against the 2026-09-22 baseline** · `docs.behindCode` found this document, `README.md`, `ADOPTION_DECISIONS.md` and `TESTING.md` older than their cited files. No floor moved today; the rows caught up with moves already made: gap-analysis 81/100 over 75 checks (abatty 0.3.3), enforced share 73% (46/63), `lib/**` coverage 98.9 / 94.5 / 99.1 / 99.3, `dup.clones` / `dup.clonedLines` 65 / 832, integration tests 65, and `valid.utcDay` (VALID.5, new in abatty 0.3) at 0. The other three documents were re-read: the index and the decisions log state nothing the version pin changed; the testing document gained the two loader exclusions and the `.claude/**` discovery exclusion.
- 2026-09-21 · **phase 13 closed** · OBS-STRUCTURED, OBS-REDACTION, OBS-CONSOLE, OBS-SIGTERM, OBS-HEALTH missing → present (OBS-TRACKER, a SHOULD, missing on purpose: decision recorded). `server/log.ts`: pino 10, JSON lines on stdout, no transport, redaction by field path (`REDACTED_PATHS`: the credentials, second factors, tokens, headers, addresses and environment secrets, at the top of a record and one level under any key; `code` at the top and under `credentials` only so `err.code` stays readable), `<redacted>` for a value, silent under a test runner; the thirteen server console calls are the logger's (a user id where an address was printed) or gone (the signer's parameters and signature, the dish actions' saved data); `no-console` at error on `lib/`, `app/`, `server/`, `auth.ts`, `proxy.ts`, `instrumentation.ts` (a client error boundary excepted). `GET /api/health` (`force-dynamic`): 200 with `SELECT 1`, 503 `unavailable` without, 503 `draining` after the signal, the two codes added to `ApiCode`. `server/drain.ts` from `instrumentation.ts` on the Node runtime: the flag flips at once, Next's own handler closes the server, waits for the pending requests and exits 143, and only a request that outlives the ten-second `unref`'d grace sees `prisma.$disconnect()` run; idempotent per module instance, with its remover. `proxy.ts` no longer rewrites `/api/*` under an admin or restaurant host (a 404 for the health probe and the dish-view beacon there; `proxy.test.ts`, four tests). `server/` is a coverage area pinned at 100 (`docs/TESTING.md`), on the server-only list and a leaf like `lib/`; the graph cruises 393 modules. **Switches proven**: `pnpm lint` red on a planted `console.log` in `lib/slug.ts` and a planted `console.error` in the health route (`no-console`), green after removal; `pnpm test` red on `email` dropped from the redaction list (two tests), on the health route no longer asking the drain (one), and on the drain no longer flipping its flag (three), green after each restore; the build is clean (five edge-runtime warnings gone once the runtime switch was the literal the bundler drops a branch on: decision recorded, `instrumentation.ts` named beside the env module); `e2e/health.spec.ts` green on the production build against the real database. **Caught on review**: the logger's header claimed pino's default destination writes synchronously; it does not (an asynchronous write flushed on exit, the one thing a frozen instance never does): the destination is now `sync: true` (a `must`); the docs presented the pool release as every stop's sequence when Next exits first on an ordinary stop; `emailVerifyToken` was the one live token column off the redaction list; the health route's `force-dynamic` reason was wrong for Next 16 (a route handler is not prerendered unless asked; the export now pins that against a default flip); `/api/health` was unreachable from an admin or restaurant host (the rewrite); five test names carried an "and" (now one behaviour each); the silent-level test depended on the shell's `NODE_ENV`; two emptied hooks in a test. **Numbers**: score 76 → 82 (74 checks), enforced share 78% → 73% (47/64) (41 hard, 6 ratchet: five observability rules the catalogue holds by review or prose join the count), readability 97 (navigability 88), 322 unit tests (51 files), 13 integration, 22 browser. **Left**: the SIGTERM drill under load on a Linux box (the review ceiling the standard names; Windows cannot send the signal); request-scoped records (no request id is bound to a child logger yet: OBS.1's "request-scoped" is the next step); an error tracker (a vendor and a DSN, the user's choice); the 57 console calls under `components/` (the browser's); the seven server-only `lib` modules not yet under `server/` (`lib/sign-in-checks` and `lib/otp-request` now import `server/log`, the two layers leaning on each other until then); whether `_next/*` assets are meant to pass the host rewrite (they are rewritten today; only the API is exempt).
- 2026-09-21 · **phase 12 closed** · CODE-ARCH-GRAPH and CODE-DEADCODE were present since phase 0 (`pnpm run graph` and `pnpm run dead` in the gate, `.dependency-cruiser-known-violations.json` empty, knip at zero), so the phase made the graph rules true for this layout and measured what was not: the template's two boundary arrows named `src/features` and `src/components` and matched nothing; now five arrows of the boundary map, written in `CLAUDE.md` ("Boundary map") and held one rule each: `lib/` imports nothing above it, a component imports `app/actions/*` only, `app/actions` and `app/api` import no component, the seven server-only modules are reached from a component through a server action only, the two portals never import each other. CODE-DUP missing → present: `abatty-probes/dup.mjs` runs jscpd 4.3.0 at its defaults (five lines, fifty tokens) over the graph's roots, tests left out, and reads `dup.clones` 82 and `dup.clonedLines` 1064 as ratchet metrics charged per file (43 files carry one); `pnpm dup` runs the same module for the table a human reads; the baseline records both (readability 100 → 97 on the navigability axis, the honest reading of 82 clones). **Switches proven**: `pnpm run graph` red on a planted cycle (`no-circular`) and on one planted import per boundary arrow (five rules, five errors, each naming its arrow), green after their removal; `pnpm run dead` red on a planted unused export (`lib/slug.ts`) and on two unimported files, green after; `pnpm run standards` red on a planted whole-file copy of a `lib/` module (`dup.clones` 83 / 82 REGRESSED, `dup.clonedLines` 1198 / 1064), green after; the two probes hold four controls (a repeated twelve-line block is one clone of fifteen lines, two unlike files are zero). **Caught on review** (no `must`): the per-file charge followed jscpd's traversal order (six clones flipped sides when the roots were passed in another order), so a clone is now charged to the lexicographically smaller of its two paths and the baseline rewritten under that definition; the probe reported the ratchet's whole file count as its scan, which would have read a moved root as "improved 82 → 0" (it now reports jscpd's own `sources`, 512, so a scan of nothing fails as such); jscpd's stderr was dropped on a failure; the run was cached for the life of the process (now per measurement); the bin path resolved one directory short; `lib/menu-loader` reaches the database client and was not on the server-only list (it is: the list is the transitive closure, and a module split into a directory stays matched); the dup script's flags were a hand copy of the probe's (the script now runs the probe module); the graph config's header was still the template's; three sentences in the docs overstated (the portals' share of the clones, the importer count, "no report when nothing is duplicated"). Also the ratchet's own catch: `CLAUDE.md` grew by the boundary map with no lesson behind it (`context.unsourcedGrowth` 5): the lesson is in `docs/LESSONS.md` (a rule copied from a template holds the template's layout). **Numbers**: score 74 → 76 (74 checks), enforced share 78% (46/59), readability 97 (navigability 88), 297 unit tests (47 files), 13 integration, 20 browser. **Left**: the 82 clones themselves (28 clones / 404 lines under the two portals' pages, whose dishes, menu, info and restaurants pages repeat 12 to 52 lines each; 50 / 612 under components, the restaurant form's basic card and the admin form the heaviest at 101 lines each); `lib/env` shared on purpose; the server-only modules not yet under `server/` (decision recorded).
- 2026-09-21 · **phase 11 closed** · CODE-JSDOC missing → present: `jsdoc/require-jsdoc` (`publicOnly`, eslint-plugin-jsdoc 64.5.4) and `jsdoc/no-types` at error over `lib/**`, `app/actions/**`, `app/api/**`, `auth.ts` and `proxy.ts` in `eslint.config.mjs`, the contexts widened from the plugin's arrow-only one to every exported declaration (function, const, default export, type, interface) after the lib worker found `export const x = z.object(...)` unseen and the reviewer `export default`. 147 blocks on 44 files (lib 103, actions 36, api 5, auth.ts 1, proxy.ts 2), a comment-only diff (no code line changed; nine line comments folded into the block they sat under, one stale trailing comment reworded), each fact traced to the code that holds it before it was written (the OTP mails' "ten minutes" names `OTP_TTL_MS`, the change-email links' fifteen minutes names the action that stores it, the 401/403 sameness of a missing and a foreign row, the restrict/cascade of every delete). DOC-FRESHNESS partial → present: the four living docs carry `source_truth` and `last_verified` (7/7 dated, 5 with sources; decision recorded on what each is verified against). **Switches proven**: `pnpm lint` red on a planted exported const without a block (`jsdoc/require-jsdoc`), on a block removed from a declared function, and on a `@param {string}` added to a block (`jsdoc/no-types`), green after each restore; the two workers proved the same pair on their own territories; `--fix-dry-run` on a removed block fixes nothing (the fixer is off). **Caught on review**: the first draft had left the plugin's fixer on (a `must`: an editor's fix-on-save would have inserted empty blocks); sixteen `z.infer` alias blocks paraphrased the schema block above them (now they say the one thing an alias knows: the parsed output); `export default` unseen by the contexts (now seen; `lib/prisma.ts` documented); two blocks overstated (`AuthError` is caught by `requireSuperAdminPage`; `credentials` is the second loose sign-in schema); and the freshness re-read the rule asks for had not happened: `docs/LESSONS.md` carried a paragraph garbled by an earlier heredoc (its escapes written as control characters) and `CLAUDE.md`, now a cited source, still said no test framework exists (both fixed in the close). **Caught, written down as it is (a comment-only phase changes no behaviour)**: `uploadArAsset(fileUrl, restaurantSlug)` receives the restaurant id from its one caller, so AR files land under `restaurants/<id>` while branding lands under `restaurants/<slug>/branding`; `resetAdminPassword` stamps `passwordResetToken: 'FORCE_CHANGE'` and nothing reads it (the next sign-in is not forced to change the password); `confirmOldEmail`/`confirmNewEmail` read no session (the token is the credential; the pages sit under the protected layout, the actions are public POSTs); the JWT session keeps the old address after a confirmed change, so `initiateEmailChange` and `updatePassword` miss until re-sign-in; **and one that needs a decision**: `Ingredient` and `DishView` are `ON DELETE RESTRICT` on `Dish` while `Dish` cascades from `MenuSubcategory` and `Restaurant`, so `deleteDish`, `deleteSubcategory`, `deleteCategory` and `deleteRestaurant` fail at the database once any dish underneath has an ingredient or a recorded view (a migration to cascade, or the actions deleting the children first: the user's call, `prisma/migrations/` being a protected path). **CI caught**: the changed-lines coverage gate (`test:changed`) reads the coverage of the files a push touched, and a comment-only touch of `lib/utils.ts` and `lib/brand-fonts.ts` (0% each, never tested) put the 44-file set under the floor (96.5 / 90.7 / 93.2 / 96.3 against 96.7 / 91.7 / 94.0 / 96.9): tests for both, and for the brand upload's two last branches; `lib/**` now measures 98.1 / 93.1 / 96.3 / 98.4 (floors unchanged: a raise is the user's call). **Numbers**: score 72 → 74 (74 checks), enforced share 77% → 78% (45/58), readability 100 (docs-freshness 100), 297 unit tests (47 files), 13 integration, 20 browser. **Left**: `components/**` not held (187 exports; decision recorded); the two `should`s the reviewer gave the integrator are in this entry and the changelog.
- 2026-09-21 · **phase 10 closed** · TEST-INTEGRATION missing → present and DATA-TENANT n/a → present: `tests/integration/**` (`vitest.integration.config.ts`, `pnpm test:integration`) runs on a real Postgres on the real migrations, every test inside a transaction that is rolled back (`db.ts`), the code under test reaching it through the client singleton (a proxy in `setup.ts`; the ORM is not mocked) and signed in as a row the test created; `scripts/ci/integration.mjs` takes `TEST_DATABASE_URL` (CI's service, a step after the gate) or starts a throwaway container, and never reads `DATABASE_URL`. `tenant-isolation.test.ts` is the negative proof DATA.3 asks for: every guard (restaurant by id and slug, dish, category, subcategory, ingredient, super admin) and the actions refuse another restaurant's rows, which stay as they were; a reorder moves none of the other's categories and counts only its own; a manager cannot delete their own restaurant; a super admin reaches every one; a signed-out caller none; a role change is seen by the next call, not by the token. `lists-and-writes.test.ts` holds the money column as Postgres stores it, a price the schema refuses before the database sees it, the sort order a write leaves behind, the restaurants list paged by keyset through its own handler, and the envelope's `forbidden` to a manager. The `lib/**` unit floors rise to the measured 96.7 / 91.7 / 94.0 / 96.9 (branches 81.3 → 91.7). **Switches proven**: `test:integration` red on a guard that lets the other tenant through, on the subcategory check removed and on the loader's restaurant filter removed, green after each restore; `pnpm test` red on a floor set above the measurement; the rollback leaves zero rows after every run. **Caught**: writing the isolation test found the hole phase 4 had deferred: `createDish`/`updateDish` took any `subcategoryId`, so a manager could place a dish under another restaurant's subcategory and onto that restaurant's public menu (`requireSubcategoryOf` in both actions, and `loadMenu` filters every subcategory's dishes by the restaurant). Also from the review: the session reset between tests, one user fixture with the hash cost named, readiness over TCP in the runner, the env probe on the ratchet's exempt list, the browser suite asserting the page inert behind a sheet (a one-in-ten axe flake made deterministic). **Numbers**: score 71 → 72 (74 checks), enforced share 76% → 77% (44/57), readability 100, 290 unit tests (45 files), 13 integration, 20 browser. **Left**: a constraint violation cannot be observed inside the rolled-back transaction (a savepoint helper is the next step); authenticated browser journeys still need a test-only source for the emailed code.
- 2026-09-21 · **phase 9 closed** · `tsc --noEmit` was already blocking in the gate and CI and green under `strict`; TYPES-STRICT partial → present: `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` on, 140 errors (53 + 86 + one cascade) → 0 with real fixes: an index read handled as the `T | undefined` it is (a first regex group, a first dropped file, an observer entry, a cursor part; `firstIssue` in the schemas, a `fire(type)` fixture and `callArgs` in the tests that name what did not happen instead of a TypeError), a typed shape where an index stood in for one (the manifest's icon triple, a per-channel colour function, the assignment field), a forwarded optional prop widened to `?: T | undefined` where a parent genuinely passes it (about 55 declarations, each chosen from its own error: decision recorded), `definedFields()` (`lib/defined-fields.ts`, tested) at the eight Prisma writes so a patch with an undefined member is the write it always was, four conditional spreads and two guards. No `any`, `@ts-*`, `as never` or `!`; two `as` casts with their reason on the line. Also, the phase 3 trigger: the shell components' portal prop is `portal` (a codemod over eight declaring components and twelve call sites, two fixture tests) and `jsx-a11y/aria-role` runs without `ignoreNonDOM`. **Behaviour proven identical**: HEAD-vs-tree differentials on the rewritten readers (colour, brand fonts, social media, password strength) byte for byte; the reviewer read every original beside its replacement and agreed the five stated non-identities are unreachable or fail-closed (a session without an email renders the login instead of a 500; an out-of-range move returns; a missing orbit part takes the default). **Switch proven**: `pnpm run typecheck` red on a planted `xs[0]` returned as a string (TS2322) and a planted `{ a: undefined }` into an exact-optional property (TS2375), green after the removal. **Numbers**: score 71 (73 checks), enforced share 76% (42/55), readability 100, 290 unit tests (45 files), 20 browser. **Left**: nothing under the flags; the `Role`-named database enum stays (it is the user's role).
- 2026-09-21 · **phase 8 closed** · The last eight files over a budget were pages (composition roots, 100 code lines; 103 to 152 each): the admin dashboard, the admin and manager create and list pages, the confirm-new-email page, the manager's dishes page and the public menu route. Each keeps its data reads, its guard and its redirects and hands its markup to `components/admin/`, `components/manager/` (one set of email-change notice primitives for both confirmation pages) and `components/shell/` (one list search, one dish row mapping and stat strip for both portals' dish pages); the menu route's metadata, viewport and loader are `lib/menu-metadata.ts` and `lib/menu-loader.ts` with tests pinning the query arguments. `size.overBudget` 8 → 0 and `size.excessCode` 228 → 0, both hard; `size.overRaw` 0; CODE-SIZE-300 partial → present. **Behaviour proven identical**: ten pages rendered old and new to static markup under mocked data in 43 states, byte for byte (one dead template expression gone). **Switch proven**: `pnpm run standards` red on a planted 102-line page (`HARD FAIL 1 / 0`, excess `6 / 0`), green after its removal. **Caught on review**: the same broken utility class two files over (`-/2`), the search form still inline in two lists (one `ListSearch` now), the admins table receiving each admin's TOTP secret (it learns whether one is set), two em-dashes carried into new files, the fixture blobs (`categoryRow`/`subcategoryRow` in the factory), a section named unlike its sibling; the phase 3 trigger to rename the `role` portal prop fired and is done as a codemod right after phase 9's flags land. **Numbers**: score 70 → 68 on the phase's head measured on 2026-09-21 (the day's rollover: the measure wrote an unindexed dated gap analysis, indexed here; 72 over 74 checks on the tree with phases 9 and 10), enforced share 76% (42/55), readability 100, 284 unit tests (43 files), 20 browser. **Left**: `components/shell/dish-list-rows.ts` outside the `lib/**` coverage floor with its own test (decision); the two list pages at 66 and 78 lines with their query inline (under the budget).
- 2026-09-20 · **phase 7 closed** · CODE-SHAPE missing → present: `max-lines` (800), `max-lines-per-function` (60; 150 in a component file), `complexity` (12) and `max-params` (4) at error over the whole tree (`eslint.config.mjs`; tests and browser specs carved out of the length rule), off only for the files `scripts/ci/shape-exemptions.json` lists, a list `scripts/codemods/shape-exemptions.mjs --write` generates from the findings, `pnpm lint` refuses when stale (`--check`) and `fn.shapeExemptions` counts: 44 → 0, hard. 72 findings (35 function lengths, 37 complexities) in 44 files → 0, by four workers on four territories in parallel: the menu page (547 lines, complexity 39 → 99, 5), shell, branding, contact, sign-in and font picker; the eight forms and the two category managers (one shared `SortableCategory`); the four uploads (one Cloudinary browser upload, one set of file checks, one progress run), the AR viewer (773 lines, complexity 49 → 99, 9), 3D page, preview and QR display; the seven longest pages, the lib helpers, `auth.ts` (`lib/sign-in-checks.ts`, 10 tests), the seed and the probes. 130 new files, none over its kind's budget; `size.overBudget` 34 → 8 (the eight pages phase 8 finishes), `size.excessCode` 5060 → 228, `size.overRaw` 1 → 0, `types.escapes` 1 → 0 (hard). **Behaviour proven identical**: each old (`git show HEAD:`) and new component rendered to static markup under the same props and forced states and compared byte for byte (69 scenarios for the uploads and viewers, 21 for the forms, the ten menu and shell components, every page in every state), a multiset diff of every class, string, attribute and tag, the seed replayed against a recording client (same calls, same order), 18,630 differential checks of the probes over the tree; the reviewer re-read the originals beside the replacements and found no dropped class, string, aria attribute, handler, cleanup or state lifetime. **Switches proven**: `pnpm lint` red on a planted five-parameter function, a 70-line function in a `.ts`, a 160-line one in a `.tsx`, and a passing file added to the exemption list (`stale exemption`), green after each restore; `fn.shapeExemptions` `HARD FAIL 1 / 0` on a hand-added entry. **Caught on review**: three same-arm ternaries and two em-dashes carried across the split, `localName` exported from a hook module, the file-header comments that `require-jsdoc` will not see (now on the exports), the `max-lines` sentence the tool contradicted (now at error). **Numbers**: score 66 → 70, enforced share 75% → 76% (42/55), readability 88 → 99 (navigability 59 → 95), 267 unit tests (39 files), 20 browser (the AR viewer page mounts and unmounts its four hooks under Playwright with no page error). **Left**: 25 new files export two to four sibling components (one concern per file, decision recorded); `components/forms/form-defaults.ts` outside the `lib/**` coverage floor with its own test; prop clumps in `SortableCategory` (13 props) and `CodeStep` (12) that one object each would halve; `iconClassName` standing for the portal at two call sites.
- 2026-09-20 · **phase 6 closed** · API.1 on this surface (five route handlers, 36 server actions, no GraphQL; the pinned catalogue wires no API-* check): one envelope (`lib/api.ts`: `{ data }` / `{ data, meta }` / `{ error, code, details? }` with a closed `ApiCode` union; `lib/api-client.ts` unwraps it in the browser and follows `meta.next`), the users and restaurants lists bounded (`limit` 1..500) and followable by an opaque keyset cursor on (sort value, id) (`lib/schemas/list.ts`; the standards-reviewer showed Prisma's own `cursor` skipping a row when the cursor came from outside the filter and ending a list on a deleted row), money as an exact two-decimal string on every crossing (the public menu, the dashboards' lists and edit pages, the dish schema's `money`, both dish forms), and every mutation returning a payload it shaped (`lib/payloads.ts`, named once per model: a User row carried the password hash, a Dish row a Decimal the wire cannot carry, which is why creating or editing a dish returned an unserialisable row). Four repository probes hard at 0: `api.bareResponse` 5 → 0, `api.unboundedList` 2 → 0, `api.rowReturn` 36 → 0 (a row returned at once or through a local, a select naming a secret column, a bare boolean), `api.floatMoney` 7 → 0. Not applicable, with the reasons written: `Idempotency-Key`, `If-Match`, versioning, an OpenAPI spec and GraphQL for an internal first-party API; the actions keep their `(id, data)` signatures; the bare-array shape was replaced with its only client in the same commit (the deprecate-first rule set aside on purpose). **Switches proven**: `pnpm run standards` red on a planted `Response.json` in a route, a planted `findMany` without a bound, a planted row return in an action and a planted `price: Number()` in a page (`HARD FAIL 1 / 0` each), green after each restore; 24 probe controls. **Caught**: the admin "Delete restaurant" button called `DELETE /api/restaurants/[id]`, a route that never existed (it calls the action); the reviewer caught the four API callers left with a throwing call and no catch (the control stayed disabled), the cursor's two edge cases, money still a float on the dashboards, `{ sent }` and `{ confirmed }` computed and dropped (the change-email form now says when its mail could not go). **Numbers**: score 66 (no API check to flip), enforced share 75% (39/52), readability 90, 207 unit tests (29 files), 16 browser. **Left**: `getMenu` (a server action) returns one restaurant's categories with no page: bounded by the tenant, outside `api.unboundedList` by its `approximates`; the upload actions answer `{ success, error }` form results rather than the envelope (form-facing, kept).
- 2026-09-20 · **phase 5 closed** · Caching: the application caches no read on the server (no store client, no `unstable_cache` or `"use cache"`, no query cache; the pinned catalogue wires no CACHE-* check for this stack), so the phase closes as **not applicable for server caches, held by a machine**: `cache.serverCacheUse` in `abatty-probes/cache.mjs` counts what creates a cached read (the three `"use cache"` forms, `unstable_cache`, `cacheLife`/`cacheTag`, a static or revalidating route export, a fetch cache option, a cache store import; an invalidation is not a cache) and is hard at 0; a first cached read re-opens CACHE.1. The two caches that exist are recorded: the manifest route's `Cache-Control: public, max-age=3600`, keyed by the slug in its URL, and the service worker (the PWA track's). CACHE.1 for the one client cache: the router cache of a preserved layout holds the dashboards' shell (every restaurant by name), and the three restaurant writes now revalidate both portal roots so a new, renamed or deleted restaurant reaches the switcher without a reload (the standards-reviewer caught the first draft removing the update's revalidation as stray: it was the one invalidation the shell had; the client applies a fresh render from the root when an action revalidated). CACHE.2: the admin layout decided the role once per mount and Next preserves a layout across client navigations, so a super admin demoted or deleted mid-visit kept reading admin pages until a full reload: `requireSuperAdminPage()` (five tests; one redirect table, the layout calls it too) is the first statement of all 17 admin pages by codemod (`scripts/codemods/guard-admin-pages.mjs`, three fixtures), and `auth.unguardedAdminPage` (probe, two controls) is hard at 0. The manager pages already scope every read by assignment. The probes moved one per module under `abatty-probes/` with their shared text readings (the single file had crossed its budget). **Switches proven**: `pnpm run standards` red on a planted `export const revalidate = 60` in an admin page and on a planted admin page without the guard (`HARD FAIL 1 / 0` each), green after each removal. **Caught**: the cached authorisation above; three `console.log` calls printing a restaurant's patch and row (contact email, phone, address) to the server log, removed. **Numbers**: score 66 (unchanged; the catalogue has no CACHE check to flip), enforced share 75% (39/52), readability 90, `size.excessCode` 5058 → 5068 (one raise, reason above), 195 unit tests, 16 browser. **Left**: the shell's restaurant list is stale for a manager whose assignment changes mid-visit (a display list; the pages redirect); caching the public menu is a design decision, not taken; the pages' `params` ids are not parsed (phase 4's probe scopes actions and route handlers).
- 2026-09-20 · `size.excessCode` 5056 → 5068 · owner: Oussama Rhoni. Reason: `requireSuperAdminPage()` as the first statement of the six admin pages already over their budget (an import and a call each; a seventh, `app/admin/(protected)/admins/page.tsx`, traded its session read for the guard and did not move): the fresh super-admin check Next's preserved layout cannot make on client navigation (CACHE.2); phase 8 splits these pages.
- 2026-09-20 · **phase 4 closed** · VALID.1: the pinned abatty has no probe for "every boundary parses", so `abatty.probes.mjs` holds the repository's own `valid.unparsedBoundary` (a route handler reading the request, a server action with an argument no parse call names, or an `authorize()` callback; declared, arrow and default exports; ten control cases, `pnpm exec abatty ratchet --controls`): 40 → 0 (36 server actions, 3 route handlers, the credentials callback), hard. Every action parses after its guard with a schema from `lib/schemas/` (`common`, `user`, `menu`, `dish`, `restaurant`; 17 tests) and the ten forms validate with the same schemas (VALID.2, sentences until an i18n layer exists: decision recorded). VALID.3: `lib/env.ts` parses the server's variables on the first server read (`serverEnv`, lazy because the module is shared with the client bundle; an empty variable is unset; a half-set mail pair refused), `lib/mail.ts` is the one Resend call site, the browser reads through `publicEnv`: `valid.rawEnv` 36 → 0, hard (the measure's remaining 1 is `playwright.config.ts`'s CI flag, a test-runner config outside the app). The reviewer found `lib/cloudinary.ts` destructuring `process.env` twice, invisible to the built-in read probe: `valid.wholeEnv` (the repository's second probe) counts the object taken whole, 2 → 0, hard, and the Cloudinary trio is `serverEnv.cloudinary`. Score 65 → 66, enforced share 75% (39/52), readability 76 → 90 (boundary-clarity 100), `size.excessCode` 5163 → 5058 (the inline parameter types and the three hand checks per upload went with the schemas). **Switches proven**: `pnpm run standards` red on a planted `process.env` read in `lib/`, a planted destructuring of it, a planted unparsed action (declared, then an expression-bodied arrow) in `app/actions/` (`HARD FAIL 1 / 0` each), green after each restore. **Caught**: changing your own password never checked the current one (the form asked, the action did not look; fixed, the compare is in the action); the change-email links printed `undefined/` without `NEXTAUTH_URL` (the auth origin, else an explicit public origin, else the send fails: the reviewer caught the first draft's fallback to the production literal); a dead `'ADMIN'` role alias in `authorize`. **The standards-reviewer also caught**: the first probe's holes (arrow, const-function and default exports unscanned; a return type's braces taken for the body; one parse counting for every argument, which let `initiateEmailChange` write an unparsed `ip` into the audit log: the dead parameter is gone), a token parse that turned a mangled link into a 500 (now the "invalid link" card), `updateDish` trusting the caller's restaurant id for the Cloudinary folder (now the dish's own, from the guard), the OTP parse inside the `try` answering the server-error message, the settings form's slug rule tightened for legacy slugs (both live slugs conform; `lib/slug.ts` makes generated ones conform), and the two sign-ins and two assignment routes as copies (the parse stays in each boundary, what follows is shared: `lib/otp-request.ts` with 3 tests, `lib/schemas/assignment.ts`). **Verified**: the full gate (build, 16 browser tests incl. a new one through the sign-in action, axe) green locally and in CI (run 35528976000); ids over the live data are all UUIDs and both slugs match the slug rule (the schemas refuse nothing that exists). **Left**: DATA-TENANT reads n/a (the probe finds no tenant column; this schema scopes by `restaurantId`), the isolation test on a real database is phase 10's; a subcategory id of another restaurant is not checked against the dish's restaurant (AUTH.1, noted for phase 10's test); the upload components still keep their own client-side type/size checks beside the server's schema (CODE.12, phase 8 splits them).
- 2026-09-20 · `size.excessCode` 5160 → 5163 · owner: Oussama Rhoni. Reason: the hydration marker the browser suite waits on (two lines in `components/menu/restaurant-page.tsx`, the file phase 8 splits) and `lib/color.ts` split out of `lib/brand-color.ts` (one import line in the branding panel).
- 2026-09-20 · **phase 3 closed** · A11Y-LINT missing → present: `eslint-plugin-jsx-a11y`'s recommended rules at error over every component with the shadcn primitives mapped to their elements (Next's preset ran six rules at warn and no mapping; it reported nothing on code that fails), 39 findings → 0 (labels naming no control, captions that were labels, three clickable spans, double-toggling rows, an autoFocus). TEST-E2E missing → present: Playwright with axe on the menu journey (load, language, dish sheet) and both sign-in pages, a phone and a desktop project, in the gate after the build and in CI on a seeded Postgres service (run 35519532521: 14 passed). A11Y-CONTRAST missing → present: `test/contrast.test.ts` / `pnpm contrast` over the tokens of both themes, 36 text pairs at AA (one was not: `--warning` on `--muted` in the light theme at 4.11:1, the token darkened two points), the focus ring at 3:1; the input border pinned at what it measures (1.46 / 1.60) with 3:1 recorded as the target. Score 59 → 65 (the measure also reads `components/menu/use-pwa.test.ts` as PWA-CONTRACT present; it tests the registration, not the shell's freshness, and the contract test proper stays with the PWA phase), enforced share 75% (39/52: FLOW-COMMITS moved from missing to partial and is counted as a review rule now). **Switches proven**: `pnpm lint` red on a planted `<img>` without alt, a `<div onClick>` and `<Button role="presentation">` through the mapping, green without; the contrast test red on `--muted-foreground` lightened to 60%; the axe scan red on a planted button with no name (button-name, critical), green restored. **Caught**: a first visit to a menu reloaded itself a second after appearing (the new worker's claim was taken for an update); the standards-reviewer then caught that the fix swallowed the change Refresh asks for, and that the reset-password menu item never received its menu-item props (both fixed, the first unit-tested both ways). The review also moved CI from `prisma db push` to `prisma migrate deploy`, which showed the migrations a year behind the schema (two `isActive` columns pushed in July 2025): the migration is written, `IF NOT EXISTS`, and proven on a scratch Postgres (deploy, diff empty, seed, deploy again a no-op); the live database has it pending; run 35521456613 applied it on the CI service and seeded through. `size.excessCode` 5158 → 5163 (two raises, reasons above). **Left**: authenticated journeys need a test-only source for the emailed code; the input border token; the three form-level double states (phase 8).
- 2026-09-20 · `size.excessCode` 5158 → 5160 · owner: Oussama Rhoni. Reason: a label must name its control (jsx-a11y `label-has-associated-control`), and the two category managers each gain the `useId()` line that gives their inputs ids; the two files are near-duplicates of each other that phase 8 merges.
- 2026-09-20 · **phase 1 closed** · ESLint findings 137 → 0 (`eslint .` over the whole tree, Next 16's core-web-vitals and TypeScript presets at their own levels, nothing held at warn); `pnpm lint` is `eslint . --max-warnings=0` and the gate's lint step runs it. What moved: 84 unused import bindings in 24 files by `scripts/codemods/remove-unused-imports.mjs` (8 fixture tests); dead props, state, handlers and a type removed with their call sites; `Date.now()` in six pages replaced by `lib/time.ts` (tested); ten components on `useSyncExternalStore` / `useEffectEvent` instead of setState in mount effects and refs read during render; the eight forms on `useWatch` / `getValues`; twelve `any`s typed (`types.escapes` 13 → 1, the one being a justified `@ts-expect-error`). Caught on the way: a random "file size" shown to users, a never-updating `useMemo` over `watch`, and a dead link the `as any` on its href had hidden. `size.excessCode` 5306 → 5158 net (one raise, 5184 → 5185, with its reason above). CODE-MAXWARN missing → present, TYPES-ESCAPES partial → present; score 57 → 59, enforced share 77%. **Switch proven**: `pnpm lint` exit 1 with a planted unused import, exit 0 without. **Verified on the production build**: the menu's language precedence (URL, then the remembered choice), the settings form's live preview and unsaved-changes guard, clean consoles; the sign-in flow's pending-credentials path (/mfa) was not exercised in a browser, and the standards-reviewer found by reading that a live storage read sent it back to the login after a successful code: fixed in 3c783a5 (the credentials are captured once on the first client render). Other review follow-ups landed there: the codemod split into three functions with three more fixtures, the worker registration out of the effect, `useMenuLocale` and `lib/device.ts` for the duplicated blocks. **Left**: the font picker's option list styles a saved font in its own face only after it is picked again on a revisit (the stylesheet link persists across client navigation and only `onload` marks it loaded); function complexity rose in four components and CODE-SHAPE (function-size rules) is phase 7; JSDoc is phase 11.
- 2026-09-20 · `size.excessCode` 5184 → 5185 · owner: Oussama Rhoni. Reason: react-hook-form's `watch()` was an inline expression in JSX; the React Compiler-compatible `useWatch` is a hook, hence a statement, one line in each of three forms already over their budget (`create-client-form`, `create-dish-form`, `edit-dish-form`); their split is phase 8. Same phase, other direction: 5306 → 5184 from the codemod and the dead code.
- 2026-09-20 · **phase 0 closed** · The switch seen both ways in the place it guards: https://github.com/oussrh/Foodify/actions/runs/35510629826 on a throwaway branch `proof/ratchet-red` (one commit placed through the GitHub API, the baseline's `types.escapes` floor lowered 13 → 12) went red at the gate step with `types.escapes 13 / 12 ratchet REGRESSED`; https://github.com/oussrh/Foodify/actions/runs/35509687312 on `main` with the floor at 13 went green on every step, the audit included. The branch is deleted. Numbers: INST-CI, INST-CI-STEPS, SEC-LOCKFILE, SEC-SECRETS missing/partial → present; three `.githooks/*` files 100644 → 100755; the pre-push hook refuses a red push and the guard refuses `--no-verify`, which is why the proof commit was created remotely.
- 2026-09-20 · **dependency upgrade, CI green end to end** · https://github.com/oussrh/Foodify/actions/runs/35509687312 on `7417ef8`: 67 pushed files, the gate green with the build suite running in CI for the first time (1 min 19 s), the changed-lines coverage green, the audit green (`pnpm audit --prod`: no known vulnerabilities, from 7 critical / 33 high in the morning). Five gated commits on `main`: latest of every current major (228ac1f), Next 16 + ESLint flat config (398caec), Prisma 7 + pg adapter (2ecb218), Tailwind 4 (308a3cc), otplib 13 / resend 6 / lucide 1.x (7417ef8). Floors: `valid.rawEnv` 37 → 36, `size.excessCode` 5307 → 5306, coverage `lib/**` raised to 93.0 / 81.3 / 79.4 / 93.3; 137 ESLint warnings from the Next 16 presets recorded for phase 1. Held back with reasons in `ADOPTION_DECISIONS.md`: TypeScript 7, ESLint 10, Prisma 8 rc, `@types/node` 26. Phase 0's remaining item is the ratchet step seen red in CI on a lowered floor.
- 2026-09-20 · **phase 0, first CI run** · https://github.com/oussrh/Foodify/actions/runs/35507179202 on `f09e306`: the gate step green in 23.8 s (lint, typecheck, import graph, dead code, 7 test files, ratchet, secret scan), the audit step red as recorded (7 critical, 33 high), the changed-lines step skipped behind it. Two corrections from the run: on a push to `main` the checkout's `origin/main` already equals HEAD, so the gate saw `0 pushed file(s)` and skipped the build suite; the workflow now passes the event's range (`before..HEAD`, or the PR base) with `--range`, and the audit step moved last so a registry red cannot skip a check of this push. Still open for the phase: the ratchet step seen red in CI on a lowered floor (a throwaway branch push), and the audit step green after the dependency upgrade.
- 2026-09-20 · **phase 2 closed** · Runner: none → Vitest 5 (`pnpm test` = `vitest run --coverage`, the gate's unit-test step). Tests: 0 → 85 in 7 colocated files over `lib/` (opening hours, social links, brand colour, pricing and locale, brand uploads, TOTP, JSON-LD), fixtures in `test/factories/`. Coverage `lib/**`, every file counted: statements 92.72, branches 79.73, functions 79.41, lines 92.94; floor pinned in `vitest.config.ts` at 92.7 / 79.7 / 79.4 / 92.9, per-file floors on `lib/menu.ts` (money display) and `lib/totp.ts` (2FA) at their measured figures, four exclusions listed there and in `docs/TESTING.md`. Rules: TEST-UNIT missing → present, TEST-COVERAGE missing → present (floor on the total, gate on the change via `pnpm test:changed`, `reportOnFailure`). Score 56 → 57, enforced share 76% → 77%. **Switches proven**: `pnpm test` exit 1 with `lib/opening-hours.test.ts` removed (four thresholds named), exit 0 restored; `pnpm test` exit 1 with `lib/totp.test.ts` removed naming the per-file floor; `pnpm test:changed` exit 1 with a planted untested three-line module under lib/ (deleted after the run), exit 0 with nothing changed. After the standards-reviewer pass: nine two-behaviour tests split, bare casts and non-null assertions replaced by typed mocks and a throwing helper, the TOTP `catch` covered by a mocked throw, `next lint` widened to `test/` and the config, default test discovery. **Left**: `app/` and `components/` have no unit floor (integration and browser suites, phases 10 and TEST.3); TEST-MUTATION (a mutation-testing tool) missing; the CI run of both steps follows the push.

- 2026-09-20 · **phase 0, instrument in place, awaiting its first CI run** · CI: `.github/workflows/checks.yml` runs `pnpm run gate` (the pre-push hook's script: lint, typecheck, import graph, dead code, unit tests, ratchet + changelog over the pushed range, secret scan, production build) and `pnpm audit --prod --audit-level=high`; `pnpm install --frozen-lockfile`; `PRISMA_GENERATE_SKIP_AUTOINSTALL`. Rules: INST-CI missing → present, INST-CI-STEPS missing → present, SEC-LOCKFILE partial → present, SEC-SECRETS partial → present. Score 55 → 53 (the merged tree's larger surface: PWA-CONTRACT now applies, FLOW-EMDASH 9 → 12 files; not this phase's files), enforced share 74% → 76%. **Switch proven**: `pnpm run -s standards -- --range main..HEAD` (the command CI runs) exit 3 with `types.escapes` floor lowered 13 → 12 in the baseline, exit 0 restored. `abatty doctor --controls`: 5 steps red on a planted violation; the three `.githooks/*` files were mode 100644 (only executable on Windows), now 100755. **Left**: no workflow run exists yet (the branch is unpushed; the agent may not push), so the CI-level red/green and its run URLs are still to be logged here; the CI audit step is red on day one (7 critical, 33 high in production deps: `next` 15.4.4 → ≥ 15.5.24, `next-auth` beta.29 → ≥ beta.32, `@auth/core` → ≥ 0.41.3, `tailwindcss-animate`'s glob/minimatch chain); the upgrade is its own change; no format step until a `.prettierrc` exists; the first CI run is on the push. INST-DEAD-CI reads partial because the `synovitec` profile assumes Woodpecker; this repository's CI is GitHub Actions (upstream report).

- 2026-09-20 · `size.overBudget` 31 → 34, `size.excessCode` 5302 → 5307 · owner: Oussama Rhoni. Reason: the harness merged into `main`, whose branding, contact and PWA work (four files: `components/menu/restaurant-page.tsx`, `app/restaurant/[slug]/page.tsx`, `components/branding/branding-panel.tsx`, `components/contact/contact-panel.tsx`) the day-0 floor was never measured on; the floor is re-measured on the tree it now guards. Same commit: `valid.rawEnv` held at 37 (`lib/env.ts` created), `docs.indexDrift` 1 → 0 and promoted to hard.
